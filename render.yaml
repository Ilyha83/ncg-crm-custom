services:
  - type: web
    name: ncg-crm-custom
    env: python
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.8
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
